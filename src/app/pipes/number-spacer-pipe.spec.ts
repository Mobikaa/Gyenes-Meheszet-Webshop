import { NumberSpacerPipe } from './number-spacer-pipe';

describe('NumberSpacerPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberSpacerPipe();
    expect(pipe).toBeTruthy();
  });
});
